{
  "name": "@includable-chrome/gmail-omnifocus",
  "version": "1.3.1",
  "description": "Add to OmniFocus for Gmail.",
  "main": "index.js",
  "repository": "https://github.com/includable/chrome-extensions.git",
  "author": "Thomas Schoffelen <info@includable.com>",
  "license": "MIT",
  "scripts": {
    "bundle": "chrome-bundle",
    "dev": "chrome-watch",
    "build": "yarn bundle && rm -rf dist/extension.zip && zip -r dist/extension.zip manifest.json dist resources",
    "prepare": "yarn build"
  },
  "dependencies": {
    "@includable-chrome/shared": "workspace:^"
  }
}