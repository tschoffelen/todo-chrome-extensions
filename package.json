{
  "private": true,
  "packageManager": "yarn@4.1.1",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach --all --parallel run build",
    "set-version": "yarn workspaces foreach --all npm version"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  }
}
