{
  "private": true,
  "packageManager": "yarn@4.1.1",
  "workspaces": [
    "packages/*"
  ],
  "version": "2.0.0",
  "scripts": {
    "build": "yarn workspaces foreach --all --parallel run build",
    "version": "yarn workspaces foreach --all --parallel run bundle $npm_package_version"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  }
}
